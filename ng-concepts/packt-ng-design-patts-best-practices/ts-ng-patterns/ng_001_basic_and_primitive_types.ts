export function primitive_eg() {
  let name: string;
  let age: number;
  let isAlive: boolean;

  name = 'Mario';
  age = 20;
  isAlive = true;

  console.log(`Name:${name} Age:${age} is alive:${isAlive ? "yes" : "no"}`);
}
primitive_eg();
